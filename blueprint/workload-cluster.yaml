apiVersion: infra.nephio.org/v1alpha1
kind: WorkloadCluster
metadata:
  name: workload-cluster
  annotations:
    config.kubernetes.io/local-config: "true"
    kpt.dev/config-injection: required
spec:
  # clusterName is injected by Nephio at package deployment time.
  # apply-replacements-owner.yaml propagates it into NFDeployment.metadata.name
  # and Interface.metadata.annotations[specializer.nephio.org/owner].
